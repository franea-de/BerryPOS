services:
  postgres:
    image: postgres:16-alpine
    container_name: berrypos-pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: berrypos
      POSTGRES_DB: berrypos
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
